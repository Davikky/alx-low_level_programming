#include "main.h"
/**
 * sub - prints difference of arguments
 *
 * @a: first argument
 * @b: second argument
 * Return: difference of a and b
 */
int sub(int a, int b)
{
	int diff = a - b;

	return (diff);
}
