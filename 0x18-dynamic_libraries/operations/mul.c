#include "main.h"
/**
 * mul - prints product of arguments
 *
 * @a: first argument
 * @b: second argument
 * Return: product of a and b
 */
int mul(int a, int b)
{
	int prod = a * b;

	return (prod);
}
